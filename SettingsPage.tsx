import ShiftPatternReport from './ShiftPatternReport';

// Inside the Settings component, in the appropriate tab or section
// Add the ShiftPatternReport component

// For example, if there's a section like:
<Box>
  <Typography variant="h6">Reports</Typography>
  <ShiftPatternReport />
</Box>

// Or add it to an existing tab content:
<TabsContent value="account">
  <AccountSettings />
  <ShiftPatternReport />
</TabsContent> 