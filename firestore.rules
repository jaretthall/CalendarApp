rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access during development
    // TODO: Restrict this before going to production
    match /{document=**} {
      allow read, write: if true;
    }
    
    // The rules below are commented out for now but should be used in production
    // Only allow writes from authenticated users
    // match /providers/{document} {
    //   allow write: if request.auth != null;
    // }
    
    // match /shifts/{document} {
    //   allow write: if request.auth != null;
    // }
    
    // match /clinicTypes/{document} {
    //   allow write: if request.auth != null;
    // }
    
    // match /syncLogs/{document} {
    //   allow write: if request.auth != null;
    // }
  }
} 